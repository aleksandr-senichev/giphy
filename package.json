{
  "name": "giphy-panel",
  "version": "1.0.0",
  "description": "Giphy Panel",
  "scripts": {
    "build": "grafana-toolkit plugin:build",
    "test": "grafana-toolkit plugin:test",
    "dev": "grafana-toolkit plugin:dev",
    "watch": "grafana-toolkit plugin:dev --watch"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "@grafana/data": "next",
    "@grafana/toolkit": "next",
    "@grafana/ui": "next",
    "@types/debounce": "^1.2.0"
  },
  "dependencies": {
    "debounce": "^1.2.0"
  }
}
